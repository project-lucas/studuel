/**
 * L'emplacement de l'onglet quand l'URL n'est pas la sienne au chargement
 * complet de l'app : vide. Lors d'une navigation dans l'app, Next y garde
 * l'onglet déjà construit (components/OngletsVivants le cache sans le détruire).
 */
export default function OngletAbsent() {
  return null
}
