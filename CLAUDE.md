@AGENTS.md

# Scolaria — guide agent

Application de soutien scolaire gamifiée (6e → Terminale), interface **entièrement en français**. Next.js 16 App Router + React 19 + Supabase (auth, Postgres, RLS) + Tailwind CSS 4.

## Commandes

```bash
npm run dev        # serveur de dev (localhost:3000)
npm run build      # build de production
npm run lint       # ESLint
npm run typecheck  # tsc --noEmit
npm test           # Vitest (tests unitaires de lib/)
```

## Architecture

- `app/` — pages App Router. Onglets principaux : `amis`, `reviser`, `defi` (central), `moi`, `tresor` ; plus `carnet` (« Mon carnet » : les dossiers de cours que l'élève écrit lui-même, ouvert depuis le bouton du titre de Réviser ; un dossier vit sous `/carnet/cours/[id]`, sa session sous `…/reviser`, l'ancien préfixe `/reviser/cours` est redirigé), `test` (sessions de quiz), `onboarding`, `login`, `compte`, `studio`, `parents` (espace parents : programme de vidéos du coach) et `admin` (studio de contenu, gardé par `is_admin`). Les mutations passent par des Server Actions (`actions.ts` à côté de la page). `app/api/` pour les rares routes API, `app/auth/callback` pour le retour d'auth Supabase.
- `components/` — composants React clients ; primitives dans `components/ui/` (shadcn/radix).
- `lib/` — **toute la logique métier est pure et testable ici** (srs, streak, xp, mastery, defi-modes, bosses, compagnon, tresor, capacity, habits, trajet…), chaque module avec son `*.test.ts` à côté. Clients Supabase dans `lib/supabase/` (`server.ts` / `client.ts`).
- `supabase/` — migrations SQL numérotées (`schema.sql`, `002_…` → `029_…`, plus les seeds de contenu `030+`), **exécutées à la main** dans le SQL Editor du dashboard, jamais par un outil. Elles doivent rester idempotentes.
- `proxy.ts` — rafraîchit la session Supabase avant chaque rendu (remplace le middleware).

## Règles du projet

- **Logique métier dans `lib/`, pas dans les composants** : fonctions pures + tests Vitest. Les pages/actions ne font qu'orchestrer.
- **Sécurité par RLS** : le serveur n'utilise que la clé anonyme Supabase. Toute nouvelle table doit avoir ses policies RLS dans sa migration.
- **Nouvelle migration = nouveau fichier numéroté idempotent** dans `supabase/` (ne jamais modifier une migration déjà exécutée) ; signaler à l'utilisateur qu'il doit l'exécuter à la main.
- **Dates** : jours = clés UTC `YYYY-MM-DD`, semaine commence lundi (index 0). Heures « élève » (créneaux de trajet) en Europe/Paris. Helpers dans `lib/time.ts`.
- **UI en français** uniquement (textes, labels, messages d'erreur).
- **Design system « crème & violet » (façon Duolingo)** : le monde visuel de l'onboarding `/bienvenue` étendu à toute l'app via les tokens sémantiques dans `:root` (globals.css). Fond **crème** `--background`, encre marine douce `--foreground`, **violet** `primary` = action/marque, **jaune solaire** `highlight`/`accent` = progression/récompense/XP, corail `destructive` = alertes, flamme ambre→orange = série uniquement. Typo **Nunito** (corps) + **Baloo 2** (titres). Utiliser les rôles sémantiques, **pas de couleurs hex en dur** hors flamme. Mondes scoping toujours en place : `.rev-*` (Réviser) et `.moi-*` (Moi) déjà violet+crème, `.onb-*` (onboarding), arène Défi : décor final « colisée doré » (`.arena-finale`) sur repli violet profond (`.defi-arena-bg`, voile violet en bas de `.arena-finale-veil`). Le mode sombre est neutralisé (`<html class="light">`). **Deux dérogations, assumées** : (1) sur l'arène `/defi`, la rangée de combat met le CTA **Duel 90 s en or** (`.olympe-gold`) entre deux tuiles sombres (`.arena-flank`) — sur un décor entièrement violet, le violet ne ressort pas, l'or si. Partout ailleurs l'or reste la couleur du gain. (2) le **tableau Progrès** de Marcel (`components/marcel/ProgresPanel.tsx`) juge la maîtrise au **feu tricolore** `success` → `warning` → `destructive` (vert/orange/rouge), et non violet/jaune : sur un écran de bilan, le jaune de la récompense se lisait comme une alarme de plus à côté du corail, et « acquis » en violet ne se distinguait pas de l'action. Ce sont des rôles existants de la DA, pas des couleurs inventées ; le violet y reste réservé aux boutons d'action (« +12 % »). Limité à ce tableau — les couronnes de Réviser gardent le violet.
- **Les outils de Marcel ont chacun leur teinte** (`data-teinte` + `.outil-*` dans globals.css) : le rail de `/marcel` mélange des modes du champ (fiche · rose, exercice · bleu, flashcards · vert) et des pages (mission · violet, méthode · indigo, oral · ambre, entraînement · corail, progrès · turquoise). **La couleur y est une IDENTITÉ, pas un rôle** — elle ne touche que l'icône, sa pastille et le liseré de la carte ; tous les boutons d'action restent violets. Huit cartes violettes ne se distinguaient pas les unes des autres, et il fallait relire chaque titre à chaque fois. C'est une dérogation étroite et documentée, comme l'or de l'arène et le feu tricolore du tableau Progrès : elle ne s'étend pas aux fonds, aux textes ni aux boutons.
- **Un dossier de matière ne montre que son programme** : les chapitres officiels en cartes (`chapters.theme`), leurs fiches dessous, rien à côté — ni axe culturel isolé, ni fiche de synthèse maison, ni chapitre hérité d'un vieux seed. Le design de référence et la marche à suivre sont dans `docs/template-matiere.md`.
- **Le duel classé est une COURSE** (`/defi/programme/[matiere]`, `components/duel/*`, logique pure dans `lib/duel/*`) : deux barres qui se remplissent à 1 000 points, la première pleine gagne, 90 s max, une question dorée (×2) par course, le rival répond EN MÊME TEMPS (ligne de temps rejouée). L'adversaire est un **vrai élève du même niveau rejoué** (table `duel_replays`, migration 351) et, à défaut, un **robot du banc** (`lib/duel/bots.ts`, 24 rivaux à tempérament, toujours marqués « IA », jamais d'établissement réel). Le serveur (`app/defi/duel-course-actions.ts`) refabrique le rival depuis la graine ou la base et n'accorde les trophées que sur SON verdict — les points ne sont jamais stockés, seuls les pas le sont. Le choix de matière (roulette + bouton DUEL, trophées par matière × jeu) ne change pas. La course est en plein écran (`lib/quiz-chrome`) et sonne « cuivre » ; ses sons propres vivent dans `lib/duel/audio.ts` (`duelSfx`).
- **Les onglets sont préchargés par `components/PrechargeurOnglets.tsx`, un par un** (règle dans `lib/precharge-onglets.ts`, mesures et protocole dans `docs/latence.md`). Ne jamais remettre `prefetch` sur les liens d'onglet : en rafale, l'app se sature elle-même (503). **On ne change d'onglet qu'en touchant son icône** : le balayage horizontal entre onglets (SwipeTabs) a été retiré le 18/09/2026, le contenu qui suivait le doigt montrait la latence de la page suivante — ne pas le remettre. Et un module `lib/*.ts` importé par un composant client n'importe ni `lib/catalog` ni `lib/supabase/*` — sinon supabase-js (57 Ko) part dans le bundle : scinder en `*-server.ts` (ex. `lib/mastery-server.ts`).
- **PostgREST rend AU PLUS 1 000 lignes par réponse, sans le dire** (réglage Supabase « max rows », défaut). Le catalogue a dépassé ce seuil le 05/09/2026 (2 323 chapitres, 2 340 quiz, 18 262 questions) : des matières pleines s'affichaient « Bientôt », la maîtrise se calculait sur 43 % des quiz. **Toute lecture sans filtre, ou filtrée par classe ou par matière, passe par `toutLire`** (`lib/postgrest-pages.ts`, pur, testé) avec un `.order('id')` ; pour compter, préférer une RPC en base (ex. `catalog_quiz_question_counts`, migration 354).
- **L'écran de chapitre range ses supports sous TROIS VERBES** (`lib/subject-template.ts` → `groupSupports`, rendu par `components/reviser/SupportChips.tsx`) : **Apprendre** (Cours, Fiche), **Mémoriser** (Flashcards, Mes erreurs), **Se tester** (Quiz, Exercice, Moi vs IA). Le Défi solo de leçon n'existe plus (il rejouait les questions du quiz sous un troisième habillage ; le mot « Défi » ne désigne plus que l'arène). **L'Exercice** est un faux contrôle noté sur 20 (`lib/exercice.ts` pur, `app/reviser/[subject]/[chapter]/exercice/*`, tables `chapter_exercices` partagée et `chapter_exercice_reponses` par élève, migration 360) **à trois niveaux** (Facile · Moyen · Difficile, migration 364) : chaque fiche porte des sujets **écrits d'avance** avec leur corrigé type (`origine = 'catalogue'`, 6e complète en migration 365), que l'IA ne fait que **corriger** en s'appuyant sur ce corrigé ; elle ne **rédige** un sujet que pour « Un autre sujet » ou une fiche sans catalogue. Le corrigé ne part jamais vers l'élève avant sa copie (`exercicePublic`). L'Exercice est **réservé à Studuel+** (le premium ne débloque pas les fiches en gemmes : il accélère seulement le gain de gemmes). **« Moi vs IA » est un bloc réservé** (`bientot: true`, aucune page derrière) laissé en place pour que Lucas décide s'il y revient. Le client IA, le quota et le lecteur JSON sont dans `lib/ia-server.ts`, partagés avec le carnet.
- **Depuis le 18/09/2026, la tuile Exercice ouvre un CAHIER D'EXERCICES** (le contrôle blanc ci-dessus devient la dernière marche du chemin, `/exercice/controle`, et reste l'écran d'un chapitre sans cahier) : **trois exercices par chapitre faits comme une page de manuel** — mise en situation, 1 à 3 **documents** (carte, graphique, texte, schéma, frise, figure, droite graduée, chaîne, circuit électrique, fiche « authentique », dialogue, Scratch, horloge, pavé), questions dessous —, notés **★ / ★★ / ★★★** (l'exercice 1 ne fait peur à personne), **débloqués l'un après l'autre**, qui rapportent **5 / 10 / 15 gemmes** et 20 / 35 / 50 XP à la première réussite. Ce n'est pas un quiz : on répond en touchant le document (`zone`), en tapant un nombre, en rangeant, en reliant, en complétant. **Le serveur est seul juge** : réponses et explications dans `exercices_cles` (aucune policy), deux essais par question (2 points du premier coup, 1 au second ; coup de pouce au premier raté, correction au second), réussi à la moitié des points (`exercice_commencer` / `exercice_verifier` / `exercice_terminer`, migration **372** ; `exercice_juger` et `exercice_normaliser` sont les MIROIRS de `lib/exercices/juger.ts` et `normaliser.ts`). Moteur pur dans `lib/exercices/` (types, validation, compilation, jugement, progression), écran dans `components/exercices/` (module CSS `manuel.module.css` : **la palette des DOCUMENTS est une dérogation assumée** — l'eau est bleue, la forêt verte, le désert sable, Scratch a ses couleurs —, elle porte une information, jamais une action ; les boutons restent violets). Fonds de carte projetés par `scripts/cartes-exercices.ts` (Natural Earth + IGN) dans `lib/exercices/cartes/`. **Le contenu s'écrit en JSON** dans `contenu/exercices/<niveau>/<matière>[.<lot>].json` selon **`docs/exercices-manuel.md`** (le guide de rédaction), se relit par `scripts/exercices-valider.ts` et `lib/exercices/contenu.test.ts` (chaque bonne réponse rejouée), se joue sans compte sur `/dev/exercices` (développement seulement), et part en base par `scripts/exercices-sql.ts` (migrations 374+).
- **Quiz et flashcards se jouent AU CHRONO, comme le duel** (`lib/quiz-chrono.ts` pur) : un budget pour la manche (12 s/question, borné 30–180 s), une bonne réponse rend 3 s, et **à zéro la manche est abandonnée sans rien écrire** (ni session, ni note, ni SRS) — pas de « fausse session » qui gonflerait la maîtrise. Le chrono du quiz se suspend pendant la lecture de la correction. Les verdicts des flashcards (« je le savais / à revoir ») alimentent la file « À revoir » sous l'identité de la question du quiz.
- **Plus de pièces (« écus ») pour le joueur** (16/09/2026) : la boucle est XP → niveaux → parfois des gemmes, et c'est tout. L'écu a quitté le bandeau (`TopHud`), la carte de profil, le catalogue des gains (`lib/gains.ts` : `UniteGain` = xp · gemme · couronne · trophee) et les écrans de fin ; le cadeau de connexion quotidien est retiré. **La Boutique est passée aux gemmes le 18/09/2026** (coffre du jour, compagnons, collection et solde en écus retirés) ; la colonne `profiles.coins` ne sert plus qu'au vestiaire d'avatar (`/moi/avatar`) — ne pas réintroduire la pièce ailleurs.
- **La Boutique (`/tresor`) est UNE page, dans cet ordre** (18/09/2026) : **Studuel+** en tête (une grande carte, `components/boutique/CarteStudueLPlus.tsx`), le **Marché** (ex-« Boost » et « offres du moment » : une vitrine FIXE de consommables utiles et pas chers — Boost XP · 2 h — migration **370**, **un seul achat par jour UTC** (migration **373**, refus `deja_aujourdhui`, la carte dit « Demain ») ; tant qu'il court, **« ×2 XP »** s'affiche contre le niveau, dans le bandeau et sur la carte joueur de l'arène (`components/BadgeBoostXp.tsx`) —, **Bouclier (PVP)** (le bouclier de trophées) qui annule la perte de la PROCHAINE défaite, un seul en réserve, consommé par `apply_game_trophies` côté serveur — migration **371**, il a remplacé le « Trophées ×2 » —, et un bloc « Fiche de révision » qui renvoie choisir sa matière ; cartes au gabarit des packs de gemmes, illustrations détourées par `scripts/marche-illustrations.mjs` dans `public/images/boutique/marche/` ; `lib/boutique/offres.ts`, `components/boutique/Marche.tsx` ; les anciens boosts — gels, gemmes ×2, double XP longs — ne se vendent plus, « personne ne les achetait »), les **capsules** au centre (plus de la moitié de la page), les **gemmes** (trois packs sur une rangée, cartes au gabarit relevé AU PIXEL sur le magasin de Clash Royale — cotes en `cqw`, rangée plafonnée à 26rem, textes blancs cernés — ; carte commune aux gemmes et au Marché, `components/boutique/CarteMagasin.tsx` + son module CSS, UNE TEINTE PAR ARTICLE (`data-teinte` : violet gemmes, ambre boost XP, bleu boost trophées, rose fiche) —, objets détourés sur un écrin clair et sobre où chacun projette SON ombre — sa propre image noircie, aplatie et couchée ; ni rayons ni halo, déjà essayés et écartés —, dans `public/images/boutique/gemmes/`, fabriqués par `scripts/packs-gemmes.mjs`, `components/boutique/RayonGemmes.tsx`, `lib/boutique/packs-gemmes.ts`, migration **369** : demande par un parent, crédit par l'admin avec `accorder_pack_gemmes`), puis **Pour ton profil** (une rangée de six objets). Chaque catégorie a **SON style de titre** façon Clash Royale (`BandeauSection`) : plaque dorée pour Boost, parchemin pour Capsules, ruban violet pour Gemmes, ruban lavande pour Pour ton profil — un même ruban partout était « trop similaire » ; pas de surtitre + titre. Ce sont des **images dessinées SANS texte** (`public/images/boutique/banniere/`, détourées par `scripts/bannieres-boutique.mjs` depuis `assets-sources/boutique-bannieres/`), posées en **neuf tranches** (`border-image`, dans `components/boutique/BandeauSection.module.css` — le premier module CSS de l'app : `globals.css` ne se recompilait plus en dev) : les bouts gardent leurs proportions, seul le centre uni s'étire (pour le parchemin, une tranche centrale de 2 % : étiré plus large, il avait l'air « étiré ») ; le titre est écrit par l'app par-dessus. Tout se paie en **gemmes**, sauf Studuel+ et les packs de gemmes (en euros). **C'est un long catalogue qui doit défiler sans latence** : tout est rendu d'un coup (jamais de `content-visibility`, qui laisse des blancs au défilement rapide), les petites images sont chargées d'emblée (`loading="eager"`), et **aucun élément fixe ne porte de `backdrop-blur`** — la barre d'onglets et le bandeau sont opaques (le flou se recalculait à chaque image du défilement : saccades et barre qui clignote). La pastille rouge de l'onglet dit « nouvelles offres cette semaine » (cookie `studuel_boutique_vue`, `lib/boutique/vue.ts`).
- **Les capsules sont des mini-formations débloquées en gemmes et lues dans le carnet** (`lib/capsules.ts` pur, `lib/capsules-server.ts`, migrations **366** tables + RPC et **367** contenu) : quatre éléments, toujours un **cours**, une **fiche récap**, un **quiz** (réussi à 60 % → capsule terminée, badge `capsule-<id>` accordé par `terminer_capsule`) et un **outil** (liste à cocher, planning ou calculateur, `lib/capsule-outils.ts`). Le contenu n'est lisible en base qu'après achat (RLS). Elles vivent dans l'étagère « Mes capsules » en tête du carnet et `/carnet/capsules/[id]` ; une capsule achetée jamais ouverte pose une pastille sur le bouton « Mon carnet ». **Certaines coûtent volontairement très cher pour pousser au paiement par carte** (`prix_euros`) : l'app n'encaisse encore aucun euro, donc « Payer par carte » enregistre une demande (`attente_paiement`, contact d'un parent) et l'admin la confirme avec `accorder_capsule` — le webhook d'un futur prestataire appellera la même chose. Prix réglables en base, sans toucher au code. **La teinte d'une capsule est une IDENTITÉ** (`.capsule-teinte-*`), comme les outils de Marcel : elle ne touche que la couverture, les boutons restent violets.
- **Le classement de l'école (onglet Amis) est un écran de ligue Duolingo AUX TROPHÉES** : un rail des six blasons de rang (`public/images/defi/ranks`, `lib/rank.ts`) où le palier courant est au centre, les paliers passés en couleur, les paliers non atteints **gris et cadenassés** (`components/amis/RailDivisions.tsx`, états calculés par `lib/amis/classement-ecole.ts`) ; puis ma place et la liste classée aux trophées (`clan_mates` renvoie `trophies` depuis la migration 362, plus `seconds`). Le compte de trophées s'écrit d'une seule façon sur tout l'onglet (`CompteTrophees` : coupe devant, nombre derrière).
- **« Modes de jeu » (le dé de l'arène) est l'écran des modes de Clash Royale** (19/09/2026) : une feuille **pleine largeur** ouverte aux 4/5, qui glisse en douceur (courbe de feuille iOS) — le haut de l'arène reste visible sous un voile **léger**, fond de feuille violet MOYEN (pas presque noir) ; languette à chevron, tap sur le voile ou glisser vers le bas pour fermer ; seule la colonne des billets est plafonnée (34rem) —, le bandeau-titre, le **total de trophées**, puis la LISTE (plus de roulette) : le **mode du jour** en grand billet à halo et une section par matière (vignette de son dossier) qui ne montre **qu'UN jeu, son jeu libre**. **Le bouton en haut à droite (grille, « +N » en or) dévoile tout le reste** : le second jeu de chaque matière (Studuel+, sous cadenas pour qui ne l'a pas), puis les **modes de l'Arène** — Blitz, Contre-la-montre, Survie, Duel fantôme, Boss, Duel en direct et Mode Coop (`/defi/jouer?mode=coop`) —, gardés mais secondaires (Lucas, 19/09/2026). Tout est calculé par `vueModes` (`lib/defi/modes-catalog.ts`).
- **La rangée de combat de l'arène : [dé] · [DUEL] · [matière], trois plaques séparées** — une soudure DUEL + matière (couture, maillon) a été essayée puis retirée le 19/09/2026, ne pas la refaire. Les deux flèches de la plaque sont le triangle d'or de Clash Royale, tournées vers l'extérieur et qui rebondissent dans leur sens (`SubjectPlate.module.css`) : elles font DÉFILER la matière, elles n'ouvrent rien. La rangée est remontée de 16 px (`5.75rem` sous l'arène et le rideau) pour que la flèche basse ne passe pas sous la barre d'onglets.
- **Le catalogue des jeux de salon a été trié le 19/09/2026 : 12 jeux, deux par matière (un seul en Maths)** — Histoire-Géo : Capitales · Frise folle ; Français : Chasse à la faute · Conjugaison éclair ; Maths : Calcul mental ; Anglais : Traduction flash · Phrase en vrac ; Espagnol : Traducción flash · Falsos amigos ; SVT : Anatomie express · Classe-moi ça ; Physique-Chimie : Chasse aux éléments · La bonne unité. Le premier de chaque matière est le jeu libre. Retirés, code compris : Duel d'orthographe, Le compte est bon (et toute la famille « tirages » : `CountdownTable`), Suite logique, Faux amis, « Pointe la carte » ; la base les connaît encore (238, 355), sans effet — `JEUX_RETIRES` (`lib/jeux/catalog.ts`) le dit aux tests miroirs. Ne pas en rajouter sans décision de Lucas (« trop de modes de jeu »). Le **Duel 90 s** (`/defi/duel`) est supprimé aussi : la course classée l'avait remplacé ; son barème survit dans `lib/duel90.ts`, que la course utilise. Les billets sont relevés **au pixel** sur Clash Royale comme les cartes du magasin (`components/defi/ModeTicket.tsx` + `ModeTicket.module.css` : corps, perforation, talon crénelé, épaisseur, cotes en `cqw` ; la feuille : `ModesSheet.module.css`), **une teinte par matière ou par mode** (`TEINTE_MATIERE`, `lib/defi/modes-catalog.ts` — une IDENTITÉ, comme les outils de Marcel). Le même billet sert l'onglet « Mode de jeu » des dossiers de matière. **Freemium : un jeu libre par matière (le premier jouable du catalogue), les autres avec Studuel+** (`lib/jeux/acces.ts` ; billet quadrillé à cadenas vers la Boutique ; garde serveur `exigerAccesJeu` sur `/defi/jeux/*`). Plus de « +XP » ni de « ×2 XP » sur les billets : jouer ne verse pas d'XP depuis la 348, les billets annoncent la RÈGLE du mode.
- **Chaque étoile de palier d'un jeu de salon rapporte des gemmes, une seule fois** : palier N → N gemmes par étoile (Éveil 1 … Maître 5, 45 par jeu), miroir pur `lib/jeux/palier-gemmes.ts`, RPC `palier_gemmes_reclamer` + table `palier_gemmes` (migration **373**). Les étoiles vivant dans le navigateur, le serveur ne paie que ce qu'il n'a jamais payé (clé unique de `gem_events`) ; la fin de partie réclame (gain qui vole vers le bandeau), la carte des paliers rattrape les étoiles jamais payées et montre les gains de chaque palier (cristal + coche).
- **Chaque mode a son ambiance à l'ouverture** : `components/defi/ModeHero.tsx` pose la scène de son billet, fondue dans sa robe `.jeu-*`, en tête de la carte des paliers (repli : la vignette de la matière sur la robe quadrillée) et de l'accueil de Blitz, Contre-la-montre, Survie, Duel fantôme et Boss.
- **L'espace PVP (course classée) a SON fond, opaque** (`components/duel/Course.module.css`) : plus de voile sur l'arène qui laissait voir le fond d'écran — le violet de l'arène éclairé dans la teinte de la matière, la **vignette du dossier de la matière** en filigrane, dans l'en-tête, sur l'écran VS, en médaillon sur la carte de fin, et promenée par la recherche d'adversaire (plus la loupe générique). Les trophées s'y comptent avec la coupe animée des Amis (`TropheeAnime`). Aperçu sans jouer : `/dev/duel?m=SVT&e=defaite|victoire|attente|vs` (développement seulement).
- Pas de commit sans demande explicite de l'utilisateur.

## Navigation du code (graphify — par défaut)

Un graphe de connaissances du projet vit dans `graphify-out/` (code + docs). **Avant de fouiller le code pour une question d'architecture** (« où est géré X », « qui appelle Y », « qu'est-ce qui dépend de Z »), **interroge le graphe** au lieu de lire/grep des dizaines de fichiers — c'est plus rapide et ça économise des tokens :

```powershell
& (Get-Content graphify-out\.graphify_python) -m graphify query "ta question"
```

- Le graphe **se rafraîchit tout seul après chaque commit** (hook `post-commit`, code uniquement, **zéro token**). Reconstruire à la main : `graphify update .` (code seul, gratuit) ou `/graphify` (complet, ré-inclut les docs).
- Graphe **local** (dans `.gitignore`). Après un nouveau clone, réinstaller les hooks : `graphify hook install`. S'il manque, le construire une fois avec `/graphify`.

## Références

- `PRD.md` — vision produit
- `README.md` — mise en route
- `docs/template-matiere.md` — **le template d'une matière** : le design de l'onglet Programme (chapitres du programme en cartes, fiches dessous), son contrat de données et la marche à suivre pour y ranger une matière de plus
- `docs/nano-banana-prompts.md` — prompts de génération des visuels (arènes, vignettes)
- `docs/exercices-manuel.md` — **le guide de rédaction du cahier d'exercices** : l'esprit (pas un quiz), la progression ★/★★/★★★, les 15 types de documents, les 8 types de questions, les fonds de carte, la relecture
